# Flowboard frontend

React + Vite + TypeScript UI for Flowboard. See the
[project README](../README.md) for how to run the whole app, and
[`_docs/specs.md`](../_docs/specs.md) for the product spec.

Quick start: `npm install && npm run dev` (serves on `http://localhost:5173`).
