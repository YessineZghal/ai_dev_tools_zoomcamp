[project]
name = "choreo"
version = "0.1.0"
description = "Choreo - a Django app for managing shared household chores (AI Dev Tools Zoomcamp HW1)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5,<6",
]
